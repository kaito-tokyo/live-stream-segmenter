---
import { transform } from "esbuild";

import LocalizedWithMapRaw from "./LocalizedWithMap.ts?raw";

export async function generateCode() {
  const { code } = await transform(LocalizedWithMapRaw, {
    loader: "ts",
    minify: true,
    target: "esnext",
  });
  return code;
}

const code = await generateCode();
---

<script is:inline set:html={code} />
